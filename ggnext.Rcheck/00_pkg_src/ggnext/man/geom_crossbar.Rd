% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-base.R
\name{geom_crossbar}
\alias{geom_crossbar}
\title{Crossbar: an interval box with the estimate marked}
\usage{
geom_crossbar(
  mapping = NULL,
  data = NULL,
  color = NULL,
  linewidth = NULL,
  alpha = NULL,
  width = NULL
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Requires \code{x}, \code{y}, \code{ymin},
\code{ymax}.}

\item{color, linewidth, alpha}{Appearance.}

\item{width}{Box width in x-axis units.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
A hollow box spanning \code{ymin} to \code{ymax} with a heavier line at \code{y} — the
compact summary used in dose-response and subgroup tables.
}
\examples{
d <- data.frame(g = c("a", "b"), m = c(2, 3), lo = c(1, 2), hi = c(4, 5))
ggnext(d, aes(g, m, ymin = lo, ymax = hi)) + geom_crossbar()
}
