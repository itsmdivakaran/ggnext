% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-layout.R
\name{geom_stream}
\alias{geom_stream}
\title{Streamgraph}
\usage{
geom_stream(mapping = NULL, data = NULL, alpha = NULL)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map time to \code{x}, value to
\code{y}, and the series to \code{color}.}

\item{alpha}{Band opacity.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Stacked areas centered on a wiggle baseline rather than zero, which
keeps every band's thickness readable as it changes over time.
}
\examples{
d <- data.frame(
  t = rep(1:6, 3),
  v = c(2, 4, 6, 5, 3, 2, 1, 3, 5, 8, 6, 4, 5, 4, 3, 4, 6, 7),
  grp = rep(c("a", "b", "c"), each = 6)
)
ggnext(d, aes(t, v, color = grp)) + geom_stream() + theme_minimal()
}
