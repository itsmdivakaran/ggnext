% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-layout.R
\name{geom_upset}
\alias{geom_upset}
\title{UpSet plot (set intersections)}
\usage{
geom_upset(mapping = NULL, data = NULL, alpha = NULL, dot_size = NULL)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Requires \code{label} (the set
membership of each observation, e.g. \code{"A&B"}).}

\item{alpha}{Bar opacity.}

\item{dot_size}{Membership-dot radius in px.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Replaces unreadable 4+ way Venn diagrams: a bar chart of intersection
sizes above a dot matrix showing which sets each bar belongs to.
}
\examples{
d <- data.frame(sets = c("A", "A&B", "B", "A&B&C", "C", "A&B", "A"))
ggnext(d, aes(label = sets)) + geom_upset() + theme_void()
}
