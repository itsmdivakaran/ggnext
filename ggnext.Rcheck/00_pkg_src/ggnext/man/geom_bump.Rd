% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-layout.R
\name{geom_bump}
\alias{geom_bump}
\title{Bump chart (rank over time)}
\usage{
geom_bump(
  mapping = NULL,
  data = NULL,
  color = NULL,
  alpha = NULL,
  linewidth = NULL,
  points = TRUE
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map time to \code{x}, rank to
\code{y}, and the series to \code{color}.}

\item{color, alpha, linewidth}{Appearance.}

\item{points}{Draw a marker at each period.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Rank trajectories drawn with sigmoid interpolation between periods, so
crossings read cleanly instead of as sharp zigzags.
}
\examples{
d <- data.frame(
  year = rep(2021:2023, 3),
  rank = c(1, 2, 3, 2, 1, 1, 3, 3, 2),
  team = rep(c("A", "B", "C"), each = 3)
)
ggnext(d, aes(year, rank, color = team)) +
  geom_bump() + scale_y_reverse()
}
