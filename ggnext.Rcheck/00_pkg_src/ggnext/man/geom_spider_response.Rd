% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-clinical.R
\name{geom_spider_response}
\alias{geom_spider_response}
\title{Oncology spider plot}
\usage{
geom_spider_response(
  mapping = NULL,
  data = NULL,
  thresholds = TRUE,
  linewidth = NULL,
  points = TRUE
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map time to \code{x}, percent
change to \code{y}, and the subject to \code{group} (or \code{color}).}

\item{thresholds}{Draw the RECIST +20\% / -30\% reference lines.}

\item{linewidth}{Line width.}

\item{points}{Draw a marker at each visit.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Percent change from baseline over time, one trajectory per subject, with
the +20\% (progression) and -30\% (response) RECIST thresholds marked.
Distinct from \code{\link[=geom_radar]{geom_radar()}}, which is a multivariate star chart.
}
\examples{
d <- data.frame(
  month = rep(c(0, 2, 4, 6), 3),
  pct = c(0, -20, -35, -40, 0, 10, 25, 40, 0, -5, -10, -8),
  subject = rep(c("S1", "S2", "S3"), each = 4)
)
ggnext(d, aes(month, pct, color = subject)) + geom_spider_response()
}
