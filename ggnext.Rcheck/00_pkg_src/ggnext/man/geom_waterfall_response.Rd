% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-clinical.R
\name{geom_waterfall_response}
\alias{geom_waterfall_response}
\title{RECIST best-response waterfall}
\usage{
geom_waterfall_response(
  mapping = NULL,
  data = NULL,
  thresholds = TRUE,
  alpha = NULL
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map the subject to \code{x}
(or omit and let ordering supply it) and percent change to \code{y}; map
\code{color} to response category.}

\item{thresholds}{Draw the RECIST reference lines.}

\item{alpha}{Bar opacity.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Subjects ordered by best percent change from baseline, one bar each,
with the +20\% / -30\% RECIST thresholds marked. Distinct from
\code{\link[=geom_waterfall]{geom_waterfall()}}, which shows a running total.
}
\examples{
set.seed(1)
d <- data.frame(
  subject = paste0("S", 1:20),
  pct = sort(runif(20, -75, 45), decreasing = TRUE)
)
ggnext(d, aes(subject, pct)) +
  geom_waterfall_response() +
  labs(title = "Best response", y = "\% change from baseline", x = NULL)
}
