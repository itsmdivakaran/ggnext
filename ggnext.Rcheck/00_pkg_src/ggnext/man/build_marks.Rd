% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{build_marks}
\alias{build_marks}
\title{Build drawing primitives ("marks") from scaled aesthetic values}
\usage{
build_marks(geom, ...)
}
\arguments{
\item{geom}{A \link{Geom} subclass instance.}

\item{...}{Method arguments; all methods take \code{scaled}, a named list of
scaled aesthetic vectors (\code{x}, \code{y} in [0, 1]; \code{color} as hex;
\code{size} as radius in px; \code{alpha} in [0, 1]).}
}
\value{
List of marks.
}
\description{
The single seam between the grammar and the renderers. Each geom method
returns a list of marks; a mark is a plain named list with a \code{type} field
(currently \code{"circle"}; the full catalog will add \code{"rect"}, \code{"line"},
\code{"path"}, \code{"polygon"}, \code{"text"}) plus type-specific fields in \emph{normalized
panel coordinates} (x/y in [0, 1], y pointing up). Because both render
targets consume marks, adding a geom never touches renderer code, and
adding a renderer never touches geom code.
}
