% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{geom_point}
\alias{geom_point}
\title{Scatter-plot layer: one point per observation}
\usage{
geom_point(
  mapping = NULL,
  data = NULL,
  stat = stat_identity(),
  color = NULL,
  size = NULL,
  alpha = NULL
)
}
\arguments{
\item{mapping}{Layer-specific aesthetic mapping (optional; merged over
the plot-level mapping, layer winning on conflicts).}

\item{data}{Layer-specific data frame (optional).}

\item{stat}{A \link{Stat} instance; defaults to \code{\link[=stat_identity]{stat_identity()}}.}

\item{color}{Literal point color (any R color spec) when \code{color} is not
a mapped aesthetic.}

\item{size}{Literal point radius in pixels when \code{size} is not mapped.}

\item{alpha}{Point opacity in [0, 1].}
}
\value{
A \link{Layer} object to add to a plot with \code{+}.
}
\description{
Draws a circle mark for each row of the layer data - the default view of
a relationship between two continuous variables.
}
\examples{
ggnext(cars, aes(speed, dist)) + geom_point(color = "steelblue", size = 4)
}
