% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-layout.R
\name{geom_chord}
\alias{geom_chord}
\title{Chord diagram}
\usage{
geom_chord(
  mapping = NULL,
  data = NULL,
  alpha = NULL,
  label = TRUE,
  label_size = NULL
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Requires \code{x} (source),
\code{xend} (target), and \code{y} (flow value).}

\item{alpha}{Ribbon opacity.}

\item{label, label_size}{Entity labels around the rim.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Entities sit on a circle; each relationship is a ribbon whose ends are
arcs proportional to the flow. Ribbon interiors are quadratic curves
pulled toward the circle center.
}
\examples{
d <- data.frame(
  from = c("A", "A", "B", "C"), to = c("B", "C", "C", "A"),
  n = c(5, 3, 7, 2)
)
ggplot3(d, aes(x = from, xend = to, y = n)) + geom_chord() + theme_void()
}
