% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{coord_polar}
\alias{coord_polar}
\title{Polar coordinates}
\usage{
coord_polar(theta = "x", start = 0, direction = 1, inner = 0)
}
\arguments{
\item{theta}{Which axis maps to angle: \code{"x"} (default) or \code{"y"}.}

\item{start}{Angle in radians for the first position; \code{0} is 12 o'clock.}

\item{direction}{\code{1} clockwise (default), \code{-1} counter-clockwise.}

\item{inner}{Inner radius as a fraction of the outer radius — use e.g.
\code{0.3} for a donut hole.}
}
\value{
A \link{CoordPolar} object to add to a plot with \code{+}.
}
\description{
Bends the panel into a circle: the \code{theta} axis becomes the angle and
the other axis becomes the radius. This is the engine behind radar
charts (\code{\link[=geom_radar]{geom_radar()}}), pie/donut wedges, and circular bar charts.
}
\examples{
d <- data.frame(g = c("A", "B", "C", "D"), v = c(4, 7, 3, 6))
ggplot3(d, aes(g, v)) + geom_col() + coord_polar()
}
