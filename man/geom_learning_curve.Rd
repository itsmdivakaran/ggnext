% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-ml.R
\name{geom_learning_curve}
\alias{geom_learning_curve}
\title{Learning curve}
\usage{
geom_learning_curve(
  mapping = NULL,
  data = NULL,
  band = TRUE,
  linewidth = NULL,
  points = TRUE
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map size/epoch to \code{x}, the
score to \code{y}, and the split ("train"/"validation") to \code{color}.}

\item{band}{Draw a ribbon between \code{ymin} and \code{ymax} when supplied.}

\item{linewidth}{Line width.}

\item{points}{Draw markers at each measured point.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Training and validation score against training-set size or epoch, the
standard read on whether a model is data-limited or over-fitting.
}
\examples{
d <- data.frame(
  n = rep(c(50, 100, 200, 400), 2),
  score = c(.75, .82, .86, .88, .70, .78, .83, .86),
  split = rep(c("train", "validation"), each = 4)
)
ggplot3(d, aes(n, score, color = split)) + geom_learning_curve()
}
