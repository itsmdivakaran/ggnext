% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-base.R
\name{geom_abline}
\alias{geom_abline}
\title{Sloped reference line}
\usage{
geom_abline(
  intercept = 0,
  slope = 1,
  color = NULL,
  linewidth = NULL,
  alpha = NULL,
  dash = NULL
)
}
\arguments{
\item{intercept, slope}{Line parameters. \verb{slope = 1, intercept = 0} gives
the identity line used in agreement and calibration plots.}

\item{color, linewidth, alpha, dash}{Appearance.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Draws \code{y = intercept + slope * x} across the panel. Like \code{\link[=geom_hline]{geom_hline()}}
and \code{\link[=geom_vline]{geom_vline()}} it ignores the plot's \code{aes()}, so it never disturbs
the data mapping.
}
\examples{
ggplot3(cars, aes(speed, dist)) +
  geom_point() +
  geom_abline(intercept = 0, slope = 3, dash = "5,4")
}
