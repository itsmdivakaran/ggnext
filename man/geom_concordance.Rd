% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-clinical.R
\name{geom_concordance}
\alias{geom_concordance}
\title{Concordance / agreement plot}
\usage{
geom_concordance(
  mapping = NULL,
  data = NULL,
  color = NULL,
  size = NULL,
  alpha = NULL
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map the two methods'
measurements to \code{x} and \code{y}.}

\item{color}{Point color.}

\item{size}{Point radius.}

\item{alpha}{Point opacity.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Scatter of two methods' measurements against the identity line, with
Lin's concordance correlation coefficient (CCC) annotated in the
corner. Unlike \code{\link[=geom_bland_altman]{geom_bland_altman()}} (difference vs. mean, for spotting
systematic bias), this reads agreement directly off how close the
cloud sits to y = x.
}
\examples{
set.seed(1)
a <- rnorm(50, 10, 2)
d <- data.frame(method1 = a, method2 = a * 0.95 + rnorm(50, 0, 0.5))
ggnext(d, aes(method1, method2)) + geom_concordance()
}
