% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{scale_x_log10}
\alias{scale_x_log10}
\alias{scale_y_log10}
\alias{scale_x_sqrt}
\alias{scale_y_sqrt}
\alias{scale_x_reverse}
\alias{scale_y_reverse}
\title{Log10 and square-root scales}
\usage{
scale_x_log10(
  name = NULL,
  limits = NULL,
  breaks = NULL,
  labels = NULL,
  expand = 0.05
)

scale_y_log10(
  name = NULL,
  limits = NULL,
  breaks = NULL,
  labels = NULL,
  expand = 0.05
)

scale_x_sqrt(
  name = NULL,
  limits = NULL,
  breaks = NULL,
  labels = NULL,
  expand = 0.05
)

scale_y_sqrt(
  name = NULL,
  limits = NULL,
  breaks = NULL,
  labels = NULL,
  expand = 0.05
)

scale_x_reverse(
  name = NULL,
  limits = NULL,
  breaks = NULL,
  labels = NULL,
  expand = 0.05
)

scale_y_reverse(
  name = NULL,
  limits = NULL,
  breaks = NULL,
  labels = NULL,
  expand = 0.05
)
}
\arguments{
\item{name}{Axis title (defaults to the mapped expression).}

\item{limits}{Optional numeric length-2 domain; \code{NULL} trains from data.}

\item{breaks}{Explicit tick positions in data units, or \code{NULL} for
automatic "nice number" breaks.}

\item{labels}{Tick labels: a character vector the same length as
\code{breaks}, a function applied to the break values (e.g.
\code{function(x) paste0("$", x)}), or \code{NULL} for automatic formatting.}

\item{expand}{Fraction of the data span to pad onto each end of the axis
(default 5\%). Use \code{0} for a tight axis.}
}
\value{
A \link{ScaleContinuous} object to add with \code{+}.
}
\description{
Shorthand for \verb{scale_*_continuous(trans = "log10")} / \code{"sqrt"}.
}
\examples{
d <- data.frame(x = 10^(1:5), y = 1:5)
ggplot3(d, aes(x, y)) + geom_point() + scale_x_log10()
}
