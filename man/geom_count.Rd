% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-base.R
\name{geom_count}
\alias{geom_count}
\title{Counted scatter}
\usage{
geom_count(mapping = NULL, data = NULL, color = NULL, alpha = NULL)
}
\arguments{
\item{mapping, data}{Standard layer overrides.}

\item{color, alpha}{Appearance.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
A scatter where the point area encodes how many observations share that
position — the fix for a scatter of rounded or discrete values where
overplotting hides the mass.
}
\examples{
d <- data.frame(x = c(1, 1, 1, 2, 2, 3), y = c(1, 1, 2, 2, 2, 3))
ggplot3(d, aes(x, y)) + geom_count()
}
