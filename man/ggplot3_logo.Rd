% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logo.R
\name{ggplot3_logo}
\alias{ggplot3_logo}
\title{Draw the ggplot3 hex sticker}
\usage{
ggplot3_logo(
  file = NULL,
  width = 520,
  dark = FALSE,
  style = c("wordmark", "monogram"),
  tagline = c("NEXT-GENERATION", "GRAMMAR OF GRAPHICS")
)
}
\arguments{
\item{file}{Output path; \code{NULL} returns the SVG as a string.}

\item{width}{Sticker width in pixels (height follows the hex ratio).}

\item{dark}{Use the dark palette variant.}

\item{style}{\code{"wordmark"} or \code{"monogram"}.}

\item{tagline}{Text under the wordmark, as one string or a character
vector of lines (two read better than one long line). \code{""} omits it.
Ignored by the monogram style, which shows \code{ggplot3} there instead.}
}
\value{
The SVG document as a length-1 character vector, invisibly when
\code{file} is supplied.
}
\description{
Generates the package logo as an SVG: a hexagon containing a small
scatter with a fitted trend, drawn with the same coordinate and color
machinery the package uses for real plots.
}
\details{
Two styles are available. \code{"wordmark"} (the default) spells out
\code{ggplot3} under the artwork with a tagline; \code{"monogram"} sets a large
\code{GG} in the middle, which stays legible at favicon and app-icon sizes
where a seven-character wordmark turns to mush.
}
\examples{
svg <- ggplot3_logo()
substr(svg, 1, 30)

# App-icon variant.
icon <- ggplot3_logo(style = "monogram", width = 256)
}
