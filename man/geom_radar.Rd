% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-layout.R
\name{geom_radar}
\alias{geom_radar}
\title{Radar (spider / star) chart}
\usage{
geom_radar(
  mapping = NULL,
  data = NULL,
  color = NULL,
  alpha = NULL,
  linewidth = NULL,
  points = TRUE
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map the axes to \code{x}, the
values to \code{y}, and the series to \code{color} (or \code{group}).}

\item{color}{Line/fill color when \code{color} is not mapped.}

\item{alpha}{Fill opacity of the polygon interior.}

\item{linewidth}{Outline width in px.}

\item{points}{Draw a marker at each vertex.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Draws one closed polygon per group across a set of categorical axes.
Pair with \code{\link[=coord_polar]{coord_polar()}} for the familiar circular form; without it the
same layer reads as a parallel-coordinates profile.
}
\examples{
d <- data.frame(
  axis = rep(c("Speed", "Power", "Range", "Cost", "Safety"), 2),
  value = c(8, 6, 7, 4, 9, 5, 9, 4, 8, 6),
  model = rep(c("A", "B"), each = 5)
)
ggplot3(d, aes(axis, value, color = model)) +
  geom_radar() +
  coord_polar() +
  theme_minimal()
}
