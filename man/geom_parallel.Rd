% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-layout.R
\name{geom_parallel}
\alias{geom_parallel}
\title{Parallel coordinates plot}
\usage{
geom_parallel(
  mapping = NULL,
  data = NULL,
  color = NULL,
  alpha = NULL,
  linewidth = NULL
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map the variable to \code{x},
the value to \code{y}, and the observation id to \code{group}.}

\item{color, alpha, linewidth}{Appearance.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Each observation is a line crossing one vertical axis per variable —
the standard way to eyeball high-dimensional structure and clusters.
Each axis is independently rescaled to [0, 1] by the stat, so
variables in different units are comparable.
}
\examples{
d <- data.frame(
  id = rep(1:3, each = 3),
  var = rep(c("a", "b", "c"), 3),
  val = c(1, 9, 4, 3, 5, 8, 7, 2, 6)
)
ggplot3(d, aes(var, val, group = id, color = factor(id))) + geom_parallel()
}
