% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-layout.R
\name{geom_ridgeline}
\alias{geom_ridgeline}
\title{Ridgeline plot (joyplot)}
\usage{
geom_ridgeline(
  mapping = NULL,
  data = NULL,
  scale = 1.6,
  color = NULL,
  alpha = NULL,
  linewidth = NULL,
  bw = NULL
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Map the value to \code{x} and
the group to \code{y} (or \code{color}).}

\item{scale}{Height of each ridge as a multiple of the row spacing;
values above 1 make ridges overlap.}

\item{color, alpha, linewidth}{Appearance.}

\item{bw}{Density bandwidth; \code{NULL} uses a Silverman rule of thumb.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
One density curve per group, offset vertically so distributions can be
compared at a glance. Groups are ordered by their factor levels, first
level at the bottom.
}
\examples{
ggplot3(iris, aes(Sepal.Length, Species)) + geom_ridgeline()
}
