% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoms-base.R
\name{geom_label}
\alias{geom_label}
\title{Text on a background plate}
\usage{
geom_label(
  mapping = NULL,
  data = NULL,
  color = NULL,
  fill = NULL,
  size = NULL,
  alpha = NULL,
  padding = NULL
)
}
\arguments{
\item{mapping, data}{Standard layer overrides. Requires \code{label}.}

\item{color}{Text and border colour.}

\item{fill}{Plate fill colour.}

\item{size}{Text size in px.}

\item{alpha}{Plate opacity.}

\item{padding}{Plate padding as a fraction of the text size.}
}
\value{
A \link{Layer} to add with \code{+}.
}
\description{
Like \code{\link[=geom_text]{geom_text()}}, but each label sits on an opaque rounded box, so it
stays readable over dense data.
}
\examples{
d <- data.frame(x = c(1, 2), y = c(2, 1), l = c("alpha", "beta"))
ggplot3(d, aes(x, y, label = l)) + geom_label()
}
